name: dws_visualisations
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy
  - pandas
  - pyproj
  - xarray
  - pip
  - jupyterlab>=4.0  # Ensure JupyterLab is up-to-date
  - pip
  - pip:
      - dash[jupyterlab]
      - plotly
      - plotly-resampler
